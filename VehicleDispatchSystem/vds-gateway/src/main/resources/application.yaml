server:
  port: 9000

spring:
  main:
    allow-bean-definition-overriding: true

vds:
  jwt:
    location: classpath:vds.jks
    alias: hmall
    password: hmall123
    tokenTTL: 30m
  auth:
    excludePaths:
      - /register
      - /login
